My minimal niri setup
- niri
- waybar
- swaylock
- swaync
- swayidle


`$ make install` - link all dirs to ~/.config/

`$ make dependencies-dnf` - install dependencies from dnf (Fedora based distros)

`$ make copy` - copy all .example files, these files are not tracked
